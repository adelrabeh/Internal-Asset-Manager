# ============================================================
# دارة الملك عبدالعزيز — OCR Platform
# الحل النهائي: التثبيت المسطح (Flat Installation)
# ============================================================

FROM node:24-slim AS base
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends \
    poppler-utils ghostscript imagemagick ca-certificates curl \
    && rm -rf /var/lib/apt/lists/*
RUN corepack enable && corepack prepare pnpm@10.26.1 --activate

# ── مرحلة التبعات ─────────────────────────────────────────────
FROM base AS deps
WORKDIR /app
COPY package.json pnpm-workspace.yaml pnpm-lock.yaml ./
COPY artifacts/api-server/package.json         ./artifacts/api-server/
COPY artifacts/ocr-dashboard/package.json      ./artifacts/ocr-dashboard/
COPY lib/db/package.json                       ./lib/db/
COPY lib/api-spec/package.json                 ./lib/api-spec/
COPY lib/api-zod/package.json                  ./lib/api-zod/
COPY lib/api-client-react/package.json         ./lib/api-client-react/
COPY lib/integrations-gemini-ai/package.json   ./lib/integrations-gemini-ai/

RUN pnpm install --no-frozen-lockfile

# ── مرحلة البناء ──────────────────────────────────────────────
FROM deps AS builder
WORKDIR /app
COPY . .
RUN pnpm --filter @workspace/api-server run build
RUN pnpm --filter @workspace/ocr-dashboard run build

# ── مرحلة الإنتاج الصافية ─────────────────────────────────────
FROM base AS production
WORKDIR /app

# 1. نسخ ملف الـ package.json الخاص بالسيرفر فقط ليكون هو الأساس
COPY --from=builder /app/artifacts/api-server/package.json ./package.json

# 2. التثبيت باستخدام npm العادي (وليس pnpm) لضمان بنية node_modules مسطحة
# نستخدم --omit=dev لتقليل الحجم و --force لتجاوز أي تعارضات في النسخ
RUN npm install --omit=dev --force && \
    npm install drizzle-kit pg connect-pg-simple --force

# 3. نسخ الملفات المبنية إلى مساراتها النهائية
COPY --from=builder /app/artifacts/api-server/dist ./dist
COPY --from=builder /app/artifacts/ocr-dashboard/dist/public ./public
COPY --from=builder /app/lib/db ./lib/db

# 4. إعداد المجلدات والصلاحيات
RUN mkdir -p /data/sqlite /data/uploads && \
    chmod -R 777 /data/sqlite /data/uploads

ENV NODE_ENV=production
ENV PORT=8080
ENV UPLOADS_DIR=/data/uploads
ENV FRONTEND_PATH=/app/public
ENV DATABASE_URL="file:/data/sqlite/ocr_database.db"

EXPOSE 8080

# 5. التشغيل باستخدام npx من الـ node_modules المسطحة التي أنشأها npm
CMD npx drizzle-kit push && node --enable-source-maps dist/index.mjs
